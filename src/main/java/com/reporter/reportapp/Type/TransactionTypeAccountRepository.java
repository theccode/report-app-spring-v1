package com.reporter.reportapp.Type;

import org.springframework.data.jpa.repository.JpaRepository;

public interface TransactionTypeAccountRepository extends JpaRepository<TransactionTypeAccount, Long> {
}
